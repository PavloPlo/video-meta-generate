# Secrets Examples and Templates
# These are example secret configurations - DO NOT commit actual secrets!

---

# Application Secrets Template
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: staging  # or production
type: Opaque
stringData:
  # Database Configuration
  DATABASE_URL: "postgresql://postgres:your-password@postgres.staging.svc.cluster.local:5432/video_meta_app"
  DIRECT_URL: "postgresql://postgres:your-password@postgres.staging.svc.cluster.local:5432/video_meta_app"

  # Session Management
  SESSION_COOKIE_NAME: "video_meta_session"
  SESSION_TTL_DAYS: "14"

  # Storage Configuration (for cloud storage)
  STORAGE_BUCKET: "video-meta-app-staging"
  STORAGE_ACCESS_KEY_ID: "AKIAIOSFODNN7EXAMPLE"
  STORAGE_SECRET_ACCESS_KEY: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
  STORAGE_REGION: "us-east-1"
  STORAGE_FORCE_PATH_STYLE: "false"

  # AI Provider Configuration
  OPENAI_API_KEY: "sk-proj-1234567890abcdef"

---

# PostgreSQL Secrets Template (Staging only)
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: staging
type: Opaque
stringData:
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "your-secure-postgres-password-change-this"
  POSTGRES_DB: "video_meta_app"

---

# MinIO Secrets Template (Staging only)
apiVersion: v1
kind: Secret
metadata:
  name: minio-secrets
  namespace: staging
type: Opaque
stringData:
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "your-secure-minio-password-change-this"

---

# Grafana Basic Auth Template (Staging only)
# Generate with: htpasswd -nb username password | base64 -w 0
apiVersion: v1
kind: Secret
metadata:
  name: grafana-auth
  namespace: monitoring
type: Opaque
data:
  auth: YWRtaW46JGFwcjEkaDc0R0xXT1MkVzMuSGV5V0xqRUlHekMuR0xXT1MuR0xXT1MK  # admin:admin (change this!)
